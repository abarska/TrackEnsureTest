package com.abarska.trackensuretest

import android.content.Context
import androidx.recyclerview.widget.ListAdapter
import androidx.recyclerview.widget.RecyclerView

//class StationItemAdapter(private val context: Context, private val clickListener: StationClickListener) :
//    ListAdapter<DataItem, RecyclerView.ViewHolder>(ItemDiffCallback()) {
//}
//
//class StationClickListener(val clickListener: (stationId: Long) -> Unit) {
//    fun onClick(station: Station) = clickListener(station.trainingId)
//}